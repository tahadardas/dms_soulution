# workflow_06_settings_admin

Goal: Deep settings for accounting/costing/inventory/printing/theme.

Steps:
1) Settings schema with versioning + audit.
2) Accounting settings (periods, chart of accounts, posting policies).
3) Cost settings (centers, allocation methods).
4) Printing settings (printers, routes, templates).
5) Theme settings (light/dark + accent).
