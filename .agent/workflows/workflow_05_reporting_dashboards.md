# workflow_05_reporting_dashboards

Goal: Dashboards + Z/X reports + KPIs + drill-down.

Steps:
1) Define KPIs (sales, margins, unit cost, cash, inventory).
2) Build dashboard views and filters.
3) Add export/print.
