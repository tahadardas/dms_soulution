# workflow_01_foundation

Goal: Scaffold baseline repo + UI tokens + auth skeleton.

Steps:
1) Initialize apps/web (React) and apps/api (Node) skeleton.
2) Add shared types/schemas package.
3) Add UI design tokens with theme switching.
4) Add auth (users/roles/permissions) skeleton.
5) Add healthcheck + version endpoint.
